export enum LogLevel {
  DEBUG = 10,
  INFO = 20,
  WARN = 30,
  ERROR = 40,
}

export enum LogCategory {
  Audio = "Audio",
  Deploy = "Deploy",
  Client = "Client",
  Task = "Task",
  Interaction = "Interaction",
  GuildVoice = "GuildVoice",
}
